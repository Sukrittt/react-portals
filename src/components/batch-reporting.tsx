import { Card, CardHeader, CardTitle } from "@/components/ui/card";

const BatchReporting = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Batch Reporting</CardTitle>
      </CardHeader>
    </Card>
  );
};

export default BatchReporting;
